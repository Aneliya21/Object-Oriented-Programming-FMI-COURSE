namespace Funcs {
	bool isDigit(char ch) {
		return ch >= '0' && ch <= '9';
	}
	bool isLowerCase(char ch) {
		return ch >= 'a' && ch <= 'z';
	}
	bool isUpperCase(char ch) {
		return ch >= 'A' && ch <= 'Z';
	}
}